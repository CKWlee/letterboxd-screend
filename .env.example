# Frontend env vars (Vite — prefix with VITE_ to expose to the browser)
VITE_TMDB_API_KEY=your_tmdb_api_key_here

# URL of the deployed FastAPI backend (Railway/Render)
# Leave blank or set to http://localhost:8000 for local development
VITE_API_URL=https://your-railway-app.railway.app
